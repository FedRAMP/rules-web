{
  "name": "cr26-site-tools",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "bun run ./scripts/deploy.ts",
    "build": "bun run ./scripts/build.ts",
    "dev": "bun run ./scripts/dev.ts",
    "fix": "bun run fix:headers",
    "fix:headers": "bun run ./scripts/fix-headers.ts",
    "sync": "./scripts/sync-rules-submodule.sh",
    "check": "bun test && bunx tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "handlebars": "^4.7.9"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
